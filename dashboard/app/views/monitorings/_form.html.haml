= form_for @monitoring do |f|
  -if @monitoring.errors.any?
    #error_explanation
      %h2= "#{pluralize(@monitoring.errors.count, "error")} prohibited this monitoring from being saved:"
      %ul
        - @monitoring.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
